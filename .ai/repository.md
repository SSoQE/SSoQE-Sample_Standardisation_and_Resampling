# Repository-specific guidance

This is a template-derived lecture repository for Sample Standardisation and Resampling.

- The current Quarto presentation and example exercise are placeholders until the lecturer approves the lesson structure.
- `Source_material/SSoQE-Golem_Engineering/` is an immutable migration copy. Verify it against `Source_material/PROVENANCE.md`; do not edit, move, delete, or modernise those files without lecturer approval.
- The legacy R files in the migration copy retain their native style for provenance. Prospective R rules apply when material is deliberately incorporated into the active lesson.
- Render through `Rscript R/render.R` only when active Quarto presentation sources change. README, adapter, project-name, and immutable source-copy changes do not require rendering.
